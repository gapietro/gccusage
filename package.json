{
  "name": "gccusage",
  "version": "0.1.0",
  "description": "Claude Code statusline tool — usage analytics, pricing, burn rate",
  "type": "module",
  "bin": {
    "gccusage": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsdown",
    "dev": "bun run src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "valibot": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsdown": "^0.9.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
