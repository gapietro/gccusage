# Audit Ledger

## Remediation log

| Date | Finding | Issue | Resolution |
|---|---|---|---|
| 2026-08-01 | REL-001 | #81 | Closed by PR #101 (squash `a324b86`) — daily cost store sharded per session |
| 2026-08-01 | COR-001 | #82 | Closed — fallback pricing generated from LiteLLM (`npm run pricing`), unpriced totals marked `?` |
| 2026-08-01 | COR-004 | #93 | Closed alongside #82 — `fetchPricing` merges the fallback on the cache-hit and failure paths, not just on fetch-success |
| 2026-08-02 | COR-002 | #83 | Closed by PR #107 (squash `de7cbe3`) — every schema field wrapped in `v.fallback`, so a bad field costs that field (one level finer than the issue's per-block proposal). Unusable payloads (invalid JSON / non-object) route to the config error line; **empty stdin deliberately stays non-error**. Array root needed an explicit guard — valibot's object schema accepts arrays and yields `{}`. Review (Bugbot) caught a **pre-existing** false `0%` context segment: `deriveContextUsage` accepted a zero token sum as a ratio; fixed in `91469b8` |
| 2026-08-02 | REL-002 | #84 | Closed by PR #106 (squash `79c0de9`) — render path does no network I/O; detached child refreshes pricing out of band. Measured live: 125ms cold, 83ms warm (was ~10.5s). **The issue's diagnosis was wrong**: the abort signal fires at 5s; the stall was process lifetime (undici holds the event loop, Claude Code waits for exit), so the proposed `Promise.race` fix would have changed nothing. Added `GCCUSAGE_PRICING_URL` |
| 2026-08-02 | SEC-001 | #91 | Closed by PR #108 (squash `b505898`) — prices bounded (finite, `input > 0`, ≤ $1000/Mtok) **inside `parseLitellmPricing`**, so `npm run pricing` inherits them when regenerating the snapshot; anchored to `FALLBACK_PRICING` at 0.1×–10× in `refreshPricing`/`fetchPricing` **only** — inside the parser the generator would anchor the snapshot to itself. Rejection is **per entry**: one poisoned model falls through to its snapshot value rather than discarding the table. `findPricing`'s fuzzy branch is now longest-key-wins (lexicographic on ties), so upstream key ordering can't swap a price; no live behaviour change, since every current model hits exact-match first. **Deliberately not done**: pinning `LITELLM_URL` to a SHA — it freezes prices and makes `refreshPricing` re-fetch an immutable file. **Accepted risk**: a legitimate >10× price move is ignored until `npm run pricing` regenerates the snapshot. Verified by re-running the generator against the live feed: zero drift, all 25 models retained. **PR review (Bugbot) caught an interaction the PR itself created**: plain longest-key-wins let a snapshot-*absent* superset alias outrank an anchored key — reproduced at **100×** on `claude-opus-5[1m]` — because making the tie-break deterministic made the poisoned key win reliably where first-match-wins had made it a coin flip. Fixed in `1ae5a99`: forward (`model.includes(key)`) matches now rank above reverse (`key.includes(model)`) ones, reverse kept as a fallback so no resolution is lost. **Residual, documented not fixed**: a poisoned key exactly equal to the model id still wins at exact-match, unanchored, capped only by the ceiling — a snapshot cannot vouch for a model it has never seen |
| 2026-08-02 | SEC-002 | #92 | Closed by PR #108 (squash `b505898`) — `readJsonValidated` joins `writeJsonAtomic` in `atomic-json.ts`, making it the single read-validate-write owner; a valibot schema per cache file, colocated with its owner. **The issue's inventory of four readers was wrong — there are five**; `block-cache.ts` was found in final review and converted too. Fixed a defect live in the shipped bundle that the issue only implied: `turn-count.json` containing the four bytes `null` made `trackTurn` deref `null` **outside** its try, blanking the whole bar (empty output, exit 0). Pricing cache is the one per-entry exception — bounds on read, never the anchor. Final review caught a **data-loss regression this work introduced**: collapsing "unreadable" into "invalid" made `migrateLegacyStore` *delete* a root-owned legacy store instead of retrying it; fixed with a three-way distinction local to that function. **Five planned tests could not fail under the regressions they named** (masking layer, in-band fixture, pre-pruned shard, gate-rejected fixture, mocked-out call graph) — all caught by review, all fixed. 520 → 576 tests |
| 2026-08-02 | OPS-001 | #85 | Closed by PR #104 (squash `c0c167f`) — `ci.yml` runs typecheck ×2 + tests on Node 22/24, and `bundle-drift` rebuilds and diffs `dist/index.js`. Guard verified by sabotage: a src-only commit turned `bundle-drift` red (and nothing else) with the annotation naming the fix. Linux and darwin bundles confirmed byte-identical, so byte-equality is sound. Spun off #105 (`engines.node >=18` contradicts a toolchain needing 22+) |
| 2026-08-02 | OPS-004 | #90 | Closed on branch `setup-robustness` (`cbe8ad7`), **shipped in PR #109, merged 2026-08-02 (squash `a2f199a`)**. `resolveStableNodePath` scans `PATH` for a `node` whose `realpath` matches the same binary as `process.execPath`, preferring a candidate whose path has no version segment; Homebrew's per-major symlink (`/opt/homebrew/opt/node@22/bin/node`) is deliberately *not* treated as version-scoped since `brew` re-points rather than deletes it. Where every candidate is version-scoped (nvm/nodenv/volta) it keeps `execPath` and warns on stdout instead of guessing. Verified end-to-end against the **shipped bundle** on this Homebrew machine (Task 6, 2026-08-02): `HOME=<tmp> node dist/index.js setup` wrote `command: '/opt/homebrew/bin/node' '.../dist/index.js'` — the symlink, not `/opt/homebrew/Cellar/node/26.5.0_1/bin/node` that `process.execPath` itself reports; `ls -l /opt/homebrew/bin/node` confirms it really is the symlink `brew upgrade` re-points. **Final review caught a regression this fix introduced for fnm users**, fixed in `90d00b5`: fnm puts `$FNM_MULTISHELL_PATH/bin` on `PATH` (`~/.local/state/fnm_multishells/<pid>_<timestamp>/bin`, older fnm `/tmp/fnm_multishells/...`), which contains **no version segment** — so the scan accepted it as "stable" and returned it with no warning, even though that directory is scoped to the shell session that ran `setup` and dies at reboot. That is strictly worse than the `execPath`+warning fallback the design intended for fnm. Now rejected by an `isSessionScoped` check on the PATH entry. The pre-fix ledger had triaged this as a cosmetic docs nit ("the comment names fnm but no test covers it") — the triage was wrong, and verifying the doc claim is what surfaced it. Same commit added an `path.isAbsolute` guard: a `PATH` containing `.` yielded the bare candidate `node`, silently degrading to the PATH-dependent resolution this whole design exists to avoid. **Deliberately not attempted**: Windows — `resolveStableNodePath` joins `node` with no `.exe` and `statusLine.command` is a POSIX shell string; the tool already assumes a POSIX shell |
| 2026-08-02 | OPS-002 | #88 | Closed on branch `setup-robustness` across two commits, **shipped in PR #109, merged 2026-08-02 (squash `a2f199a`)**. `2d19e71` makes `readExistingSettings` refuse to proceed when `settings.json` parses to `null`, a scalar, or an array root — previously `null`/scalar threw inside the property assignment and were swallowed by `main().catch(() => process.exit(0))`, and an array root didn't throw at all: the assignment silently succeeded and `JSON.stringify` dropped the `statusLine` key on write. `f99c817` scopes that blanket catch to statusline mode only, so the CLI path (`args.length > 0`) now surfaces the throw via `console.error` + exit 1 instead of reporting success. **The issue's own Fix section and acceptance criteria disagreed**: the Fix section proposed handling only `null`/scalar; the acceptance criteria implied a bad parse should also be refused. The implementation took the acceptance criteria and extended refusal to unparseable JSON too — a behavior change beyond the issue's literal Fix text — because the old behavior for malformed JSON was worse than either: catch the parse error, warn, and continue with `settings = {}`, silently clobbering the rest of the file. Verified against the shipped bundle (Task 6): `null` → stderr `gccusage: <path> contains null, not a JSON object. Fix or move it, then re-run \`gccusage setup\`.`, exit 1, file byte-unchanged, no `.bak`; unparseable JSON (`{ not valid json`) → same refusal pattern, no `.bak`. **Open weakness, not closed**: the plan's sabotage-guard test ("statusline still exits 0 on a forced error") was dropped as unwritable — every render-path I/O site is individually defended, so there is no reachable sabotage that throws past it to prove the outer catch still degrades gracefully. Separately, `cli-exit-code.test.ts`'s `"still exits 0 on the success path"` test passes identically before and after `f99c817` and would not catch an over-correction that scoped the new try/catch too broadly (e.g. wrapping the whole `main()` again). **Final review overruled the instinct to delete it**: it asserts exit 0 *and* `setup complete` on stdout by spawning the real bundle, so it fails on a load crash, on breakage that only appears under bundling (a documented hazard here — vitest's resolver papers over what a real spawn catches), and if the new `exit(1)` ever became unconditional. It is the only real-spawn coverage of the setup success path. What was wrong was the **claim** about it — the plan called it the guard against over-correction, which it cannot be — not the test; the plan text was corrected in `90d00b5`. The over-correction itself is guarded structurally instead: the new try/catch sits inside the `args.length > 0` branch and `main().catch(() => process.exit(0))` is byte-for-byte untouched, verifiable from the diff |
| 2026-08-02 | OPS-003 | #89 | Closed on branch `setup-robustness` (`2d19e71`, consuming `writeFileAtomic` extracted from `writeJsonAtomic` in `714e8f5`), **shipped in PR #109, merged 2026-08-02 (squash `a2f199a`)**. `runSetup` now writes `settings.json` through `writeFileAtomic` (temp file + rename) and, on the success path, first writes `.bak` holding the pre-existing file's exact original bytes — the case that previously got no backup at all. **The issue's literal acceptance criterion — "the file is never observed in a partial state" — is not testable from this suite**: that would require killing the process mid-write and inspecting the filesystem. What's tested instead is the mechanism: POSIX rename is atomic, and the `.bak` write happens strictly before the real write and is verified byte-identical to the original (Task 6 re-ran this against the shipped bundle: `diff` between the pre-run file and the post-run `.bak` reported no difference). **Sabotage note from implementation**: the plan's Task 3 Step 5 "Sabotage B" — literally, "move the `.bak` write above the `readExistingSettings` call" — was not executable as written, because that line reads `existing.raw`, a value `readExistingSettings` produces; moving it above would reference `existing` before its definition. The implementer substituted an equivalent unconditional backup-then-validate ordering that exercises the same property (a refused file must get no backup), confirmed it failed as expected, and reverted |
| 2026-08-02 | — | #105 | Closed on branch `setup-robustness` (`9554655`), **shipped in PR #109, merged 2026-08-02 (squash `a2f199a`)**. `engines.node` raised from `>=18` (contradicted by `devEngines`'s own `>=23.6.0` floor, and impossible in practice for `scripts/*.ts`, which Node runs directly and which needs native type stripping) to `>=22`; `devEngines.runtime.version` set to `>=22.18.0` — the release where type stripping runs unflagged — kept at that exact figure per the plan rather than "corrected" to `22.1` or `23.6`; `tsdown.config.ts`'s `target` raised `node18` → `node22`; the CI workflow comment's now-false claim ("`engines.node` still says >=18, untested here and tracked separately") replaced with one stating the 22 floor plainly. Bundle diff before/after the `target` change: byte-identical (`diff` on copies taken immediately before/after rebuild) — this codebase's syntax surface apparently needed no `node18` downlevelling to begin with, so there was nothing to drift. **Final review found the issue was not actually closed by that commit**, fixed in `90d00b5`: the most user-facing statements of the floor were untouched — `README.md` said "Node.js 18+" in two places and, at line 454, reproduced the exact two-numbers-in-the-tree ambiguity the issue was filed about ("runs on Node 18+ … those need 23.6+"), while `package-lock.json`'s root entry still carried `engines.node: ">=18"`, which the next `npm install` would have rewritten as unrelated churn in some later PR. All four now say 22 |
| 2026-08-02 | CACHE-001 | #96 | Closed by PR #110 (squash `540d962`). `computeCacheKey` hashes the whole stdin payload plus terminal width; `checkCache`/`writeCache` take that single key and `key` is required in the entry schema, so an old-format entry fails validation and misses rather than being served. Built **by subtraction, not enumeration** — everything is in the key unless explicitly removed — which is the part that answers the issue's actual complaint (the old tuple fell behind because widgets grew inputs nobody added to it). **The issue's literal fix ("hash the render-relevant inputs") is a trap taken at face value**: `cost.total_duration_ms` and `total_api_duration_ms` are recomputed on every spawn, so hashing the payload naively misses on *every* render and re-reads the whole transcript set each time (#94) — it deletes the cache rather than fixing staleness. Both excluded; `session-timer`/`api-latency` stay TTL-bounded, invisible on a counter that is already a duration. A test asserting a duration-only tick still *hits* is the counterweight and fails if the key is later widened to the full payload. **Deliberately not done**: git branch / worktree status, which the issue also lists — they are on disk, not in stdin, so keying on them means spawning git on every invocation including the cache hits the cache exists to make cheap. All 14 new tests passed on first run and were therefore each sabotage-checked; the sharpest catch was the aliasing mutation (`rest.cost` reused instead of copied), which would have deleted `total_duration_ms` from the *caller's* payload and silently broken burn-rate. Verified against the shipped bundle: `NORMAL`→`INSERT` at identical cost and width switches immediately, and a duration-only tick leaves the cache file untouched. 617 tests |
| 2026-08-02 | PERF-001 | #94 | Closed on branch `perf/today-transcript-read` (Tasks 1–7). `buildRenderContext` parsed every transcript touched today on every cache miss — measured on the real corpus (253 files, 115.1 MB) at 0.16s before vs 0.06s after (`/usr/bin/time -p`, 3 runs each, statusline cache cleared between runs; before = merge-base bundle `git show 540d962:dist/index.js`, after = this branch's build). **The root finding, not in the issue**: that 33 MB read was entirely discarded in the shipped default config. `metrics.today` had **one** consumer repo-wide (`src/cli.ts:51`), and `cli.ts` never calls `buildRenderContext`; no widget reads it. `calculatedTodayCost` is used only under `costSource: "calculated"` — the default is `"auto"`, where today's spend comes from the daily store, not the transcripts. **The issue's own suggested fixes (incremental offsets, mtime skip, dedupe the double aggregation) all optimised a computation that mostly should not run at all** — the actual fix was to stop running it, then make the remaining `calculated`-mode path cheap. Changes: (1) `aggregateTokens(entries)` replaces the two-array form; `AggregatedMetrics.today` is deleted outright rather than left reading zero, so a future widget must add the plumbing deliberately instead of getting a plausible false `0`. (2) transcripts are read only when `settings.costSource === "calculated"` — gated on the *setting*, not the resolved source, since `"auto"` with no stdin cost resolves the session source to calculated while today's spend still comes from the store. (3) `src/cache/today-aggregate-cache.ts` reuses a per-file aggregate whenever `(mtimeMs, size)` are both unchanged, so the calculated path re-parses only files that actually grew; `gccusage today` shares it, output verified byte-identical against the pre-change binary. A cached entry is reused only after `(mtimeMs, size)` re-verify against the live file — same no-lock posture as the daily cost store: a racing statusline can cost an extra re-parse but never serves a wrong total. **Measured at the issue's stated sizes** (synthetic 20-file corpora, `costSource: "calculated"` forced via `XDG_CONFIG_HOME`, calculated-mode engagement confirmed each run via the aggregate-cache file count): 35 MB → cold 0.15s / warm 0.06s / warm 0.06s (repeat cold runs settled to ~0.10s once the OS page cache warmed from the corpus write); 350 MB → cold 0.52s / warm 0.08s / warm 0.08s (repeat colds ~0.47s). Warm stayed flat (0.06–0.09s) across a 10x corpus-size increase — the acceptance criterion — while cold grew ~4.7x on the raw first-run numbers and ~8-9x once the ~0.05s fixed node-startup cost is subtracted from both; the sub-linear raw ratio is fixed overhead, not a caching defect, and is called out here rather than smoothed over. A CI test (Task 6) pins bytes-parsed as flat across a 10x corpus. 640 tests |
| 2026-08-02 | REL-003 | #87 | Closed by PR #112 (merged 2026-08-02, squash `366fdbe`). `readStdin` resolved `""` on timeout, indistinguishable from "Claude Code sent nothing", so a slow payload rendered a confident `$0.00` beside a non-zero `Today:` from the daily store. Now returns `{raw, timedOut, timeoutMs}`; deadline 1s -> 5s (`GCCUSAGE_STDIN_TIMEOUT_MS` overrides, the seam that lets a test drive the shipped bundle); `index.ts` writes `formatStdinTimeout` and returns **before** `parseStatusJson`/`runStatusline`, so the cache is untouched — on the old code the empty-object bar was *written to* the cache under the empty payload's key, so a second timeout inside the TTL served the wrong bar without reading stdin. **Two host facts from the 2.1.220 binary drove the design and were not in the issue**: Claude Code waits **600s** for the statusline command (`e.timeout ? e.timeout*1000 : xm`, `xm = 600000`), so the 1s deadline was entirely self-imposed; and **empty stdout erases the bar** (`statusLineText = undefined`) rather than preserving the previous one — the old bar survives only on the abort path — which ruled out declining to render as a silent option. **Deliberate scope**: clean EOF with zero bytes stays silent (preserves `gccusage < /dev/null`, pinned by a test); partial bytes report a timeout, not a parse error, since the host `end()`s stdin immediately so bytes-without-`end` mean truncation; `parseStatusJson` untouched. **Found in final review, not the plan**: `resolveTimeoutMs` guarded `NaN` but not `setTimeout`'s 2^31-1 ceiling — `GCCUSAGE_STDIN_TIMEOUT_MS=99999999999` passed, Node clamps the timer to ~1ms and warns to an invisible stderr, so *every* render would have become the degraded line. That is the exact failure the adjacent comment claimed the guard prevented. Fixed in `019cd47`. All 7 planned sabotages turned a specific named test red; 5 re-derived independently rather than taken on the implementer's word. Verified against the real binary: `sleep 7 | /usr/bin/time -p node dist/index.js` -> `real 5.03` with `within 5s`. 639 -> 661 tests; `stdin-reader.ts` 0% -> covered, closing one of the six files in #95. **Still open, deferred deliberately**: the stream `error` path rejects into the blanket catch and blanks the bar with no message — same defect class, different trigger, needs its own issue |
| 2026-08-03 | COR-003 | #86 | Closed by PR #114 (merged 2026-08-03, squash `f2f125d`). `visibleLength` measured `String.length` — UTF-16 code units, not terminal columns — so CJK counted 1 where it draws 2 and the bar overflowed. New `src/utils/display-width.ts` owns all Unicode reasoning: `Intl.Segmenter` for grapheme clusters, `get-east-asian-width` (3rd runtime dep) for columns. **The issue named one defect; there were three.** `truncateAnsi` carried a *second* guard, `stripAnsi(str).length <= maxWidth`, that returned overflowing strings untouched — fixing `visibleLength` alone would have left the issue's own repro passing through unchanged (34 columns, `length` 17, `<= 20` -> returned as-is). It also walked per UTF-16 unit, so it could split a surrogate pair. `trimTo` sliced by code point, stripping combining marks and orphaning ZWJs. **Ambiguous = 1 column**, with `{ambiguousAsWide: false}` passed *explicitly*: every decorative glyph the bar draws (`▶` U+25B6, `…` U+2026, `│` U+2502, powerline U+E0B0/U+E0A0) is EAW=Ambiguous, so treating them as wide shifts every measurement. The option is passed rather than defaulted because the package's runtime default is narrow while its own JSDoc declares `@default true` — a live hazard, verified in the bundled source. **Two exceptions found by measuring, not reasoning**: VS16 clusters count 2, and Regional Indicators are EAW=**Neutral** so a flag measures 1 from its base code point and needs its own rule. **The regression guard that made this safe**: every glyph in the default bar is ASCII or Ambiguous, both 1 column either way, so the pre-existing exact pins (88/80/79 in `default-layout-width.test.ts`) had to stay byte-identical — any shift meant the Ambiguous policy was wrong. They never moved. **Three tests in this branch initially proved nothing** and were each caught and replaced: (1) the planned shrink test put a ZWJ family at the *end* of the string, where `trimTo` consumes it first — verified vacuous by dumping ~4200 (text, overflow) combinations under both implementations and diffing; they differ on exactly one class, an emoji-presentation sequence with a trailing character (`"feature/x<U+2764 U+FE0F>z"` -> cluster `feature/x…` vs code-point `feature/x<bare U+2764>…`). (2) the acceptance test asserted with `visibleLength`, the function under test, so under the revert it validated the broken ruler against itself and passed while the line really measured 62 columns against a 45 budget; now asserts with `displayWidth`. (3) the plan predicted the `>` -> `>=` mutation would fail the straddle test; it fails `never splits a grapheme cluster` instead — the mutation IS caught, by a different boundary. Final review ran 10 mutations, all 10 caught. **Perf, and the plan's reasoning was wrong about it**: measured +7.3ms mean on the render path, under the 10ms rule pre-registered before the number was known, so no fast path ships. But the cost is **one-time `Intl.Segmenter` construction at module scope** (~7.3ms; 25.3ms empty process vs 32.6ms constructing one and doing nothing), not per-call work (~2.2µs). The ASCII fast path the plan contemplated and rejected would have recovered ~zero, since the constructor runs on import regardless; the only real lever is lazy construction *plus* a whole-string simple-text path. Spec corrected. **Deliberately not done**: `stripAnsi` matches only SGR, so non-SGR escapes (reachable via `custom-command`'s arbitrary shell output) still count as visible text — filed as **#113**. Final review also found `truncateAnsi`'s tail comment claimed "Unreachable" when a malformed CSI reaches it; the overflow there is pre-existing and belongs to #113, so only the comment was corrected. 661 -> 691 tests |

| 2026-08-03 | — | #113 | Closed by PR #116 (merged 2026-08-03, squash `ca272e6`). Spun off #86. `stripAnsi` matched SGR alone, so an OSC-8 hyperlink measured 37 columns for the 4 it draws and the bar truncated early. **The issue named one direction; there were two** — `truncateAnsi` never called `stripAnsi`, it hand-rolled `indexOf("m", i)`, and that under-measured: `ESC[2Jhome` at maxWidth 4 returned the *entire input plus an ellipsis*, three columns charged nothing, a string longer than the one it was asked to shorten. Two recognisers disagreeing is the root cause, so the fix is the shared seam (`escapeLengthAt` in `terminal.ts`), not the wider regex. C0 controls handled with it; TAB/LF/ESC excluded with reasons at the definition. Incomplete escape = visible, deliberately (over-measure truncates early, under-measure overflows). Hand-rolled grammar rather than `ansi-regex`: ECMA-48 is frozen unlike the Unicode data behind `get-east-asian-width`, and `ansi-regex` had a ReDoS advisory on this pattern shape while the input is arbitrary shell output — linearity measured, not asserted (400k chars, 1.1-7.4ms). Verified on the shipped bundle: 9 columns with the label truncated away before, 20 with it intact after; default bar byte-identical at five widths. **Three findings came from the sabotage battery, not the plan**: M4 falsified this work's own spec (alternation order is not load-bearing — the lookahead is), two written tests were vacuous (a single CR cannot catch a stateful `g`-regex, and neither can two *non-adjacent* controls, since any non-matching cluster between them resets `lastIndex`), and stripping LF collapsed the two-line bar into one 90-column line. **Deliberately not done here**: injection — correct measurement does not stop `ESC[2J` reaching a bar embedded in Claude Code's TUI; filed as #115 and closed by PR #117, next row. 691 -> 719 tests |
| 2026-08-03 | — | #115 | Closed by PR #117 (merged 2026-08-03, squash `7209fae`). Spun off #113. Correct measurement did not stop the bytes: `custom-command` output reached stdout verbatim, into a bar embedded in Claude Code's own Ink TUI. `sanitizeAnsi` in `terminal.ts` is an SGR-only allowlist, applied in `renderer.ts` to **every** widget output at both collection sites — free, because no widget emits ANSI of its own (colour arrives later from `fg`/`bg`), and it covers `git-branch`/`project`/`cwd`/`model` too. **"SGR" is narrower than "a CSI ending in `m`"**: the grammar's parameter class `[0-?]` includes the private markers `< = > ?`, so `ESC[>4;2m` — xterm `modifyOtherKeys`, which reconfigures how the terminal reports keypresses — would have walked through the allowlist. **The design missed 8-bit C1 entirely**: U+009B *is* CSI and U+009D *is* OSC, parsed by VTE terminals in UTF-8 mode, so a 7-bit-only allowlist inspecting `ESC` alone still cleared the screen; caught by the final whole-branch review, not by design and not by any per-task review, which each saw only one task's diff. Three rules deliberately invert `stripAnsi` and are documented at the definition: an incomplete escape is **dropped** here (measuring keeps it because over-measuring only truncates early; emitting must drop it, or an unterminated `ESC[2` is completed into a screen-clear by the next literal `J` later in the bar — there is a test for that cross-widget splice), LF dropped (per-segment layer, before `renderFull` joins lines), TAB → one space. OSC-8 dropped, with the cost recorded at the sanitiser; the hyperlink's visible label survives as ordinary text between two escapes. Trailing `ESC[0m` when SGR survives, `endsWith`-guarded for idempotence, since chalk closes only fg/bg per segment. **The spec's own claim was wrong and is corrected in-tree**: ordering the sanitise before `isSeparatorOutput` was necessary but not sufficient — `cleanSeparators` only collapses a separator adjacent to another or at a boundary, so an emptied `custom-command` between two widgets laid out as a bare padded segment; `isEmptyOutput` keys on the exact `text === ""` contract, **not** `.trim()`, which would swallow a whitespace-configured `custom-text`. **Two written tests were vacuous**: the CR row passed either way because `custom-command` calls `.trim()` before the sanitiser runs, and the OSC-8 expectation was simply wrong. Perf cleared by measurement: 3.4us/call, ~34us/render against 60-70ms. 719 → 770 tests |

| 2026-08-03 | — | #103 | Closed by PR #119 (merged 2026-08-03, squash `803e9d3`). Spun off #82. `parseLitellmPricing` read four base fields and discarded the feed's `*_above_200k_tokens` tier, so long-context sessions under-costed in `calculated` mode. **The issue's framing was wrong**: it proposed waiting for LiteLLM to publish the tier, but the feed already publishes it for five long-context Sonnet keys — verified live 2026-08-03 — and gccusage was throwing it away. A 300k-prompt Sonnet 4.5 session was billed $1.05 and is now $2.02 from rates already in the cache file. `claude-opus-5` publishes none, so its premium tokens still price at standard rates but are now **flagged** rather than passed off as complete; inventing a rate stays refused (#82). **The constraint that shaped everything: the tier is billed PER REQUEST, not per session.** Applying it to a session aggregate over-charges about as badly as ignoring it under-charges (50 turns of 60k each is 3M cumulative input at standard rates), so the split happens in `aggregateTokens` where per-entry usage survives, and `TokenMetrics.premium` is a **subset** of the four base counts (standard = total − premium) so every token-count widget keeps seeing full totals. **No `[1m]` parsing anywhere** — `findPricing` already resolves `claude-opus-5[1m]` forward to `claude-opus-5`, and the flag falls out of "has premium tokens, has no premium rate", so future 1M models are covered and the flag self-clears when LiteLLM publishes. `approximated` is deliberately NOT `unpriced`: unpriced means usage is *missing* from the total (`(partial)`), approximated means the total is a *lower bound* (`(approximate)`) — reusing the existing sentence would have stated something false. **Anchoring drops where sanitising strips**, and the asymmetry is documented at the definition: anchoring has the snapshot to fall back to, sanitising has nothing (human-adjudicated when the review flagged it as a defect). **The final whole-branch review caught the one defect no per-task review could see** — the same pattern as #115: `pricing.json` had no version, so a cache written by the OLD parser would shadow the tiers this PR adds to the snapshot for up to the 24h TTL, and print "no premium rate is published for them" about a model whose rate ships in the snapshot. Task 5 versioned one on-disk cache; nobody owned the other. Now versioned, failing closed to a snapshot that has the tiers. Also fixed there: the CLI's billed figure counts cache tokens while `Total Tokens` two lines up does not (scope now named in the sentence), and negative counts are rejected at the cache schema since `calculateCost` now *subtracts* premium. **Two plan errors caught in pre-flight**, before any agent ran: a test file the plan said to create already existed, and the migration test as written would have passed vacuously (an entry keyed on a vanished path never reaches the total, so it proves nothing about validation). Verified on the shipped bundle offline with a cold cache: `claude-opus-5[1m]` at 320k renders `$0.42?`, `claude-sonnet-4-5` at 300k renders `$2.02` unmarked. Spun off **#118** (`cache_creation_input_token_cost_above_1hr` dropped the same way). 770 → 810 tests |

| 2026-08-03 | TEST-001 | #95 | Closed by PR #120 (merged 2026-08-04, squash `065cf82`). **Merged with red checks, and they were never a test failure**: all four CI jobs died in ~3s on "The job was not started because recent account payments have failed or your spending limit needs to be increased" — a GitHub billing block on the account. A `gh run rerun` produced the identical annotation. Verified by hand instead, twice: first by cloning the pushed branch to a clean tree and running the four jobs including `npm ci`, then again on merged `main` — typecheck, 850 tests, `npm run coverage` exit 0, `dist/index.js` byte-identical to a fresh build. **Still unverified on CI's Node 22/24 matrix** (local runs were Node 26), so the first green run after billing is restored is the real confirmation. **The issue's numbers were 10 PRs stale and re-measuring came first**: overall was 97.83%, not 87.78%, and five of the six named files had been fixed in passing (stdin-reader 0→97 by #112, cli 17→90 by #109, pricing-cache 18→97 and pricing-fetcher 35→86 by #108, custom-command 46→82). Two real gaps remained. `index.ts` (0%) is the only module that runs on **import** rather than on call, which is exactly why nothing reached it — tested by importing the real entry module with its five collaborators stubbed, **deliberately not** by extracting a testable `main()`, since the top-level `main().catch(() => process.exit(0))` *is* the global error handler the audit named and moving the body out would have left that one line untested. `flex.ts` (54%) ships `left` by default so its other three modes were unmeasured. The gate is `thresholds: {perFile: true, statements: 70}` — per-file because a global average is precisely what hid this (87.78% overall while every defect sat in a file at 0–46%). **Type-only files are excluded because v8 scores a file with no executable statement as 0%, not 100%**, which would fail the threshold for a file that cannot be tested; the list is enumerated, not `src/types/**` (`block-metrics.ts` lives there and exports a real constant), and `type-only.test.ts` transpiles each entry with tsc and fails if it emits any JS — an `enum`, `namespace` or parameter property all *read* as type syntax and all emit code. **The concurrency test is the part that nearly shipped vacuous**: run against the real pre-#101 tracker (`git show a324b86^`, rebuilt into the bundle) it failed with "no shard directory" — a layout mismatch, not a lost update — because it read only the shard layout. It now reads the pre-shard layout too, so the failure says "6 of 8 sessions survived", and so it would still fail if someone consolidated the shards back into one read-modify-write file under the *current* name, which is REL-001 returning. 14 mutations across `index.ts`/`flex.ts`, all 14 caught; a 15th appeared to survive and had simply never applied (its regex missed the real indentation) — worth re-checking rather than recording as a surviving mutant. Gate itself sabotage-checked: removing `flex.test.ts` turns it red naming the file. 810 → 850 tests, lowest file `block-cache.ts` at 78.12% |
| 2026-08-04 | — | #118 | Closed by PR #121 (merged 2026-08-04, squash `e07f55c`). Spun off #103. `parseLitellmPricing` dropped `cache_creation_input_token_cost_above_1hr` AND `normalizeEntry` dropped the transcript's nested `cache_creation` breakdown — both halves had to move for either to matter. **The issue was filed conditionally** ("whether the transcript even records which TTL was used needs checking before this is actionable"); it does, on **98,722 of 98,722** usage-bearing lines locally, 45,032 with a non-zero 1h count. Measured impact **+7.53%** corpus-wide, **+10.1%** on Fable 5 — not the rounding error the issue implied. **Two things the issue did not anticipate**: the feed publishes a **cross-product** field (`..._above_1hr_above_200k_tokens`, on the three sonnet-4-5 keys), making cache creation a 2x2 matrix over {5m,1h} x {standard,above-200k}; and the feed's 1h value is **wrong for 2 of the 23** models publishing it (`claude-3-opus` prices a 1h write BELOW its own 5m write; `claude-3-haiku` is 20x), both carrying a copy-pasted `claude-3-7-sonnet` rate. Modelling `cacheCreation1hTokens` as a **subset** of `cacheCreationTokens` (as `premium` sits inside the base counts) makes the matrix fall out with no special-casing — `premium` is itself a `TokenCounts` so it carries its own 1h subtotal, and `above200k` is itself a `RateSet` so it carries its own 1h rate. Missing rates derive `input x 2`: matched **exactly by 21 of 23** published records and by all three published cross-product rates, so it is existing derivation policy (`x 1.25`, `x 0.1` already ship) rather than #82's forbidden invention. A published rate below its 5m sibling is **repaired, not rejected** — the field is required and has no safe absence state, so rejecting would drop a whole model over one bad sibling and regress #92's per-entry posture. **Monotonicity only**, deliberately: a band would catch haiku's 20x too but goes stale and would reject a genuine repricing (#91's accepted risk); haiku's bad value is pinned by a test asserting it SURVIVES, so a future band swap fails loudly. **FIVE vacuous fixtures were found and fixed in review, four of them defects in the plan I wrote.** Three in Task 1 (numbers where the correct and buggy answers coincided — e.g. the cross-product expectation `1.2e-5` equalled both `base 1h x 2` via the repair path AND `tierInput x 2` via derivation, so read/misread/derive all agreed). One in Task 3, caught by the whole-branch review running actual mutations: the test *named* "the 2x2 matrix" set `premium` identical to `metrics`, so the `standard` bucket was all zeros and the new subtraction line evaluated to 0 whatever it computed — a sign flip and a copy-paste both passed **866/866**. **The fifth was created BY one of the fixes**: giving the v1-cache test's fixture the new field (correct, to isolate the version literal) removed the incidental `COST_KEYS` bounds coverage it had provided, after which deleting `"cacheCreation1hCostPerToken"` from `COST_KEYS` outright left the suite **fully green**. Only a whole-branch mutation sweep found that. **The e2e test also lied**: it claimed to exercise "the shipped bundle" but imported `runStatusline` from `src/`, which vitest resolves happily — the repo's real pattern is `pricing-blackhole.test.ts`'s `execFileSync` against `DIST`. Now proven to drive the bundle by a two-step control: sabotage `cost-calculator.ts` with `dist/` left STALE and the test PASSES; only after `npm run build` does it fail. Both on-disk caches invalidated (`PRICING_CACHE_VERSION` 1→2; `today-aggregates.json` via a required field), and every other persisted artifact audited for token counts or rates — none carry either. **Merged with red checks again, same GitHub billing block as #120** (all four jobs, 2-3s, **zero steps executed**); hand-verified on a clean clone (`npm ci`, typecheck, 871 tests, coverage, bundle md5 identical) and again on merged `main`. **Node 22/24 still unverified — the machine has only Node 25/26 and no version manager**, so that debt now spans #114 through #121. Spun off #122 (concurrency ENOTEMPTY flake), #123 (blackhole 500ms budget flake), #124 (snapshot tier monotonicity gap), #125 (latent tier-stripping from an inflated base 1h rate). 850 → 871 tests |
| 2026-08-04 | — | #122 | Closed by PR #126 (merged 2026-08-04, squash `b534e0c`). **Merged with red checks for the third time — the same account-level GitHub billing block as #120 and #121** (all four jobs, 3-4s, zero steps executed); hand-verified on a clean clone of the pushed branch and again on merged `main` (typecheck ×2, 871 tests, coverage exit 0 at 97.83%, bundle md5 `73cb904b`). Spun off #121. **The issue's guess at the cause was too generic** ("a child outlives the assertion phase"): the culprit is specifically the pricing refresher `maybeSpawnPricingRefresh` spawns `detached`, `unref`'d and stdio-ignored (PR #106, for #84). It returns no pid, so nothing in the test can await it, and it writes into the directory `afterEach` deletes. **One cause, two failure modes, and only one was filed**: a write landing during `rmSync`'s walk raises `ENOTEMPTY` (the reported ~1-in-6), and a write landing *after* it silently resurrects the directory — that second mode was happening on **every** run, evidenced by **41 stale `gccusage-concurrency-*` directories in /var/folders**, each containing exactly the `gccusage/pricing.json` the grandchild wrote after the delete returned. Fix seeds a recent attempt stamp so the refresher returns before the spawn — the only way to bound that child from the test, since it cannot be awaited or killed. `PRICING_REFRESH_STAMP_FILE` is **exported rather than hardcoded**, closing a vacuity hole: a rename would otherwise leave the test seeding a path nothing reads and silently get a refresher back while still passing. A refused-port `GCCUSAGE_PRICING_URL` is defence in depth and also stops a daily-cost test contacting the **live pricing feed**, which it had been doing from 8 concurrent grandchildren. `expectNoRefresherSpawned` is the guard — the parent rewrites the stamp immediately before spawning, so suppression breaking is detected deterministically rather than as a 1-in-6 flake. **Reproduced deterministically before fixing** by pointing the refresher at a slow local endpoint, which showed `rmSync` succeeding and `gccusage/pricing.json` reappearing afterwards |
| 2026-08-04 | — | #124 | PR #127 (merged 2026-08-04, squash `3f5c0dd`). Spun off #121. **Merged with red checks for the fourth time — the same account-level GitHub billing block as #120, #121 and #126** (all four jobs, 3-12s, zero steps executed); hand-verified on merged `main` (typecheck clean, 871 tests, bundle md5 `8ec7c0f4` identical to a fresh build). **Node 22/24 still unverified, now spanning #114-#127.** The snapshot sanity sweep asserted `1h >= 5m` on the base rates but the tier arm checked only finiteness and positivity, so a regenerated snapshot with `above200k.cacheCreation1hCostPerToken` below its own `above200k.cacheCreationCostPerToken` would have passed. **`isSaneTier` does not make that comparison either** — it compares each tier rate against the BASE's (`COST_KEYS.every((key) => rates[key] >= base[key])`), never the tier against itself, so neither the parse-time validator nor the snapshot test covered the relation. Unreachable today (`parseTier` routes the tier rate through the same `resolveCache1hRate` repair against that same tier's 5m rate), so this pins a currently-correct invariant rather than fixing a live defect. **Verified by breaking what it guards**: setting one tier's 1h rate to `5e-6` against its `7.5e-6` 5m rate produced exactly one failure naming the new assertion, with no other assertion disturbed; reverted, 871/871 |
| 2026-08-04 | — | #125 | PR #127 (merged 2026-08-04, squash `3f5c0dd`). Spun off #121. **Merged with red checks for the fourth time — the same account-level GitHub billing block as #120, #121 and #126** (all four jobs, 3-12s, zero steps executed); hand-verified on merged `main` (typecheck clean, 871 tests, bundle md5 `8ec7c0f4` identical to a fresh build). **Node 22/24 still unverified, now spanning #114-#127.** Documentation, not code, and deliberately so. D2 admits an implausibly-high **base** 1h rate by design (monotonicity only, no plausibility band); since `cacheCreation1hCostPerToken` is in `COST_KEYS`, a model in that shape that also published an above-200k tier lacking the cross-product field would derive its tier rate as `tierInput x 2`, land below the inflated base, and have its **whole tier stripped** — losing long-context pricing and being flagged `approximated` because its *base* rate was bad. Latent: no feed model has that shape (all five tier-carrying keys are base `6e-6` / tier `1.2e-5`; `claude-3-haiku` has no tier). Not suppressed, because the only fixes are a band (rejected by D2, reasons still hold) or exempting the key from `isSaneTier` (loses real protection for a case that does not occur). **Both of the issue's code claims were re-verified against source rather than trusted**; both hold. **Knock-on the issue did not name**: `resolveCache1hRate`'s doc comment claimed the consequence was recorded there *instead* of in D2 — writing the D2 paragraph falsifies that sentence, so it now points at D2. That comment survives bundling, so `dist/index.js` moved for a comment-only `src/` change — a reminder that the bundle-drift job is byte-equality and cannot be skipped on "just a comment" |
| 2026-08-04 | — | #123 | Closed by PR #126 (merged 2026-08-04, squash `b534e0c`). **Merged with red checks for the third time — the same account-level GitHub billing block as #120 and #121** (all four jobs, 3-4s, zero steps executed); hand-verified on a clean clone of the pushed branch and again on merged `main` (typecheck ×2, 871 tests, coverage exit 0 at 97.83%, bundle md5 `73cb904b`). Spun off #121. **The 500ms budget was a machine-speed assertion in disguise, and measuring said so**: a render costs ~117ms here, of which ~25ms is bare `node -e ""` startup and the blackhole itself accounts for **1ms** — so the budget left ~4x headroom on a figure ~99% unrelated to the property under test, and it failed at 531ms under load. Replaced with a comparison against a control render whose endpoint **refuses immediately**, which cancels machine speed out because both are spawned Node processes on the same machine moments apart. A refused port is the right control for exactly the reason the file already documented about unroutable addresses (it fails fast, so broken code looks fast) — and it writes nothing, so it does not import #122's resurrection bug into this file. **Separate cache slots per render** are load-bearing: one render's attempt stamp would otherwise put the other inside the 10-minute backoff, so the second would never spawn a refresher and the comparison would measure nothing. An absolute 5s ceiling stays as a backstop for the case the diff is blind to — a build where *both* renders stall. **The margin is 1000ms against a 10520ms regression**, an order of magnitude, versus the old 4x. Sabotage: reintroducing the #84 shape (an awaited fetch on the render path) fails it at **10082ms vs 127ms for the control**, the control holding flat proving it isolates the right variable. Ten full-suite runs green with zero new leaked directories; five runs of the timing test with all 16 cores saturated, green |
| 2026-08-04 | CLEAN-002 | #99 | Closed by PR #128 (merged 2026-08-05 UTC, squash `581ec70`). **Merged with red checks for the fifth time — the same account-level GitHub billing block as #120, #121, #126 and #127** (all four jobs, 4-11s, zero steps executed); hand-verified on the branch and again on merged `main` (897 tests ×2, coverage per-file `src/config/layout.ts` 100% / `src/data/turn-tracker.ts` 98.27% against the 70% gate, bundle-drift clean). **Node 22/24 still unverified, now spanning #114-#128.** Both halves of the issue shipped: `trackTurn` is gated on `layoutIncludesWidget(settings, "turn-counter")` (new `src/config/layout.ts`), and the store moved from one global `turn-count.json` to per-session shards under `turns/`, pruned on a session's **first render only** so sharding does not trade a per-render write for a per-render `readdir`. `shardKey` — the regex-or-sha256 rule that keeps a stdin session id off the filesystem path — was extracted to `src/utils/paths.ts` so both stores share one implementation. **The issue's "every render" is wrong**: `runStatusline` returns from the statusline cache before the pipeline runs, so `trackTurn` only ever fired on a cache *miss* — which also means **`turnCount` has never counted turns**, it counts cache misses, and the widget's `#` label has always overstated. Filed as follow-up rather than fixed. **Three things the plan did not anticipate, all found by review.** (1) `TurnDataSchema`'s `count: v.number()` accepted `Infinity` (`JSON.parse("1e400")`), so a corrupt shard rendered **`#Infinity`** in the bar — demonstrated against *unmutated* code, inside the very `describe("no NaN survives a hostile cache directory (#92)")` block that exists to stop it. Pre-existing on `main`; fixed here (`v.pipe(v.number(), v.safeInteger())` on `count` and `updatedAt`) because this branch rewrites that schema. (2) **Gating silently made an existing test vacuous**: the hostile-payload leg rendered with `DEFAULT_SETTINGS`, which has no `turn-counter`, so its turn-store sabotage became unreachable while the test kept passing. Repaired with a layout that includes the widget. (3) **The plan's own anti-vacuity mutation was itself a no-op** — swapping `TurnDataSchema` for `v.any()` changes nothing for a `"null"` payload, because `readJsonValidated` collapses schema-rejected and validated-null to the same JS `null` and `trackTurn`'s `existing &&` guard short-circuits either way; the implementer diagnosed this rather than accepting the PASS, and substituted removing the guard, which does discriminate. **A claim this branch made and then had to retract**: the legacy `turn-count.json` unlink sits *inside* the gated `trackTurn`, so it only ever runs for the minority who enable the widget — every existing user has that file and most keep a ~50-byte orphan forever. Corrected in both the code comment and the design spec rather than ungating the unlink, which would put I/O back on every render. Four stale docs describing the retired single-slot store were rewritten, including a fixture-regeneration runbook whose steps were actively wrong |
| 2026-08-04 | MAINT-002 | #100 | Closed as resolved (no PR — verification only), at `e9b2123`. The structural cause is gone: `writeFileSync` appears **exactly once** in non-test `src/`, inside `atomic-json.ts:24`, so every persistence write is atomic by construction rather than by remembering; `readJsonValidated` joined `writeJsonAtomic` there in #92, one owner with a valibot schema per cache file. **Two of the issue's five rows no longer exist as described** — `src/data/turn-tracker.ts` was deleted outright by #132 (the turn count is derived from the transcript, so its store is gone), and `block-cache.ts` was converted during #92's final review, which is where that issue's "four readers" became five. A sixth reader added later (`today-aggregate-cache.ts`, #94) uses the shared boundary too, which is the outcome this issue asked for. **Two hand-rolled reads deliberately remain and are named rather than hidden**, because "no raw `JSON.parse` anywhere" is not what shipped: `daily-cost-tracker.ts:113-129` keeps a three-way unreadable/invalid/valid distinction the two-outcome helper cannot express — collapsing it caused the root-owned-legacy-store data-loss regression during #92 — and still validates with a schema; `pricing-refresh.ts:28-39` is a genuine residue **carrying a live defect, filed as #133**. **That second one is this issue's own thesis proving out rather than a coincidence**: the argument was that five duplicated blocks mean atomicity and validation each get remembered separately, "hoping the sixth cache file added later remembers" — `pricing-refresh.ts` IS that later file (PR #106, for #84), and it remembered atomicity (`writeJsonAtomic`) while forgetting validation |
| 2026-08-04 | — | #133 | **Filed, not fixed** — found while verifying #100 for closure, so the verification paid for itself. `attemptedRecently()` reads the refresh backoff stamp with a bare cast whose `typeof stamp?.timestamp !== "number"` guard **admits `Infinity`**: `JSON.parse('{"timestamp":1e400}')` is `Infinity`, `Date.now() - Infinity` is `-Infinity`, and `-Infinity < REFRESH_BACKOFF_MS` is true, so the backoff latches on. **Permanently**, because the stamp is only rewritten *after* that check (`if (attemptedRecently()) return;` at :57 precedes `writeJsonAtomic` at :64) — the file causing the wrong answer is the one the repair path is gated behind, so nothing self-heals and the only recovery is deleting the cache file by hand. Since the render path never fetches (#84/PR #106) and the detached child is the only thing that ever updates `pricing.json`, the effect is prices frozen at whatever is on disk or at the bundled snapshot, drifting from reality with no error, no marker and no log — the silent-wrong-answer class. **Narrower than first measured, and the correction matters**: only `+Infinity` latches; `-Infinity` and `0` correctly fail open. An initial check said otherwise and was wrong because `JSON.stringify` turns `Infinity` into `null`, so the test never fed the parser the bytes it claimed to — re-run against raw file contents instead. **Third instance of this exact shape in the repo** (`#Infinity` from `turn-count.json` in #128 review; the `v.finite()`/`v.safeInteger()` constraints in #128 and #131), and `daily-cost-tracker.ts:29` already carries a comment naming this precise hazard. Fix is to route the stamp through `readJsonValidated`, not to add a fourth bespoke `Number.isFinite` check |

## Verification — 2026-08-04 @ e9b2123 (Node matrix)

**The Node 22/24 debt spanning #114–#132 is closed.** It had been recorded as
unverifiable locally — the dev machine has no version manager and only Node 26 —
and **that premise was simply false**. Official tarballs need no version manager:
fetch `node-vX-darwin-arm64.tar.xz` plus `SHASUMS256.txt` from
`nodejs.org/dist/<version>/`, `shasum -a 256 -c`, extract, prepend its `bin` to
`PATH` for one command. Versions resolved the way `actions/setup-node` does
(latest of the major, via `nodejs.org/dist/index.json`): **22.23.2** (npm 10.9.8)
and **24.19.0** (npm 11.17.0).

**Deliberately not `fnm`**, though it was the obvious tool and was authorised:
putting fnm on `PATH` changes what a real `gccusage setup` writes, because
`resolveStableNodePath` has an fnm-specific `isSessionScoped` rejection branch
(#90). Verifying the build must not perturb the thing being verified. The
tarballs leave nothing outside a scratch directory.

All four CI jobs, run per version from two fresh `git clone`s, exit codes
captured rather than eyeballed:

| Job | Node 22.23.2 | Node 24.19.0 |
|---|---|---|
| `npm ci` (validates lockfile, rebuilds via `prepare`) | rc=0 | rc=0 |
| `npm run typecheck` | rc=0 | rc=0 |
| `npm run typecheck:scripts` | rc=0 | rc=0 |
| `npm test` | rc=0 — 901 tests / 55 files | rc=0 — 901 tests / 55 files |
| `npm run coverage` | — (CI runs this once, off-matrix) | rc=0 — 97.83% statements |
| `bundle-drift` | rc=0 | rc=0 |

Suites were run with `HOME` and `XDG_CACHE_HOME` pointed at scratch dirs: that
mirrors CI's empty runner home and stops tests touching the real
`~/.cache/gccusage`, which running under the developer's own `HOME` would.

**Bundle byte-equality now also holds across Node versions, not only across
platforms** (the latter was confirmed in #85). `dist/index.js` built under 22,
under 24, and the committed artefact built under 26 are all md5
`0235a48c0053fdb6e1705e568a40d148`. So tsdown's `node22` target downlevels
nothing on this syntax surface and `bundle-drift` pinning one Node version costs
no coverage — worth re-checking after any tsdown bump, since it is an
observation, not a guarantee.

**Still open, and not fixable from here:** the GitHub Actions billing block, now
spanning **eight consecutive merges** (latest run `30969659109` on `main`, four
jobs, zero steps executed, same "recent account payments have failed" annotation
as #120/#121/#126/#127/#128). The local matrix run above substitutes for the
Node-version half of that gap but not for the Linux-runner half; the first green
CI run after billing is restored remains the real confirmation.

**Dependency advisories checked in passing and deliberately not acted on.**
`npm audit` reports 9 (2 low, 7 high); all seven highs are build-chain only and
none ship in `dist/index.js`. `valibot` is the one real runtime dependency
flagged, and neither advisory is reachable: the ReDoS (GHSA-vqpr-j7v3-hqw9)
covers `>=0.31.0 <1.2.0` while the direct dep is pinned at 1.2.0 — the high is
`rolldown/node_modules/valibot@1.0.0`, a nested build-time copy — and the
`record()`/`flatten()` advisory (GHSA-5qjj-4xww-7phc) needs `flatten()`, which
the repo never calls (`v.record()` is used in `today-aggregate-cache.ts:55` and
`pricing-cache.ts:38`; `v.getDotPath` appears once, in `config/loader.ts:90`, on
a schema with no `record()` and only to build a string). **`npm audit fix`
proposes tsdown 0.9 → 0.22.14, a semver-major bundler bump** — that rewrites
`dist/index.js`, trading a CI invariant for a cosmetic fix on code that never
ships. If tsdown is bumped, do it deliberately with the bundle diff reviewed,
never as audit remediation. Version-only triage would have called the ReDoS live
here; checking the vulnerable range against the *installed* version and grepping
prod `src/` for the named API is what showed it wasn't.

## Remediation — 2026-08-05/06, after the grade at 7e6becd

| Finding | Commit | Resolution |
|---|---|---|
| CLI-001 | `79d8eb3` | `--version`/`-v`/`--help`/`-h` recognized; `src/version.ts` mirrors package.json with a drift test. 4 sabotages caught |
| OPS-007, DOC-001 | `b148b97` | 1.0.0 released — tag `v1.0.0`, GitHub release, CHANGELOG with a Known Limitations section, README badges restamped from the 15-commit-stale `0802.8` |
| — | `e6ba60e` | `docs/` (24 plans/specs, ~13.7k lines) untracked ahead of publication; nothing referenced it. Still readable from history — accepted |
| **OPS-005** | *repo made public* | **CI executed for the first time ever.** Public repos get free standard-runner minutes, so the billing block that spanned 12 merges simply stopped applying. Branch protection also became available (it had returned `403 Upgrade to GitHub Pro or make this repository public`). `main` now requires all 5 checks, strict, force-push and deletion blocked, `enforce_admins: false` by the owner's choice — so the gate binds contributors and PRs but not the owner, which is the one reason the cap is not *fully* discharged |
| — | `ca564b0` | Enabling protection broke `build-badge.yml`, which committed to `main` on every push (`GH006 … hook declined`) and misreported it as "another run landed first". Replaced the decorative build counter with GitHub's live CI badge: no commits, and it reports something real. 4 sabotages caught |
| **TEST-002** | `6be3b5f` | Refresher suppression swept across all spawn tests via a shared seam. **0 leaked dirs and 0 network writes on a full run, from 7 and 7** (1,520 had accumulated). 3 sabotages caught |

**Two audit claims corrected during the TEST-002 fix, both by measurement.**
(1) `cli.test.ts`/`cli-exit-code.test.ts` never leaked — only the statusline path
reaches `pipeline.ts:48`. The real leakers were `statusline-width`,
`stdin-timeout` and `stdin-resilience`. (2) An absent stamp in a leaked
directory reads like proof the refresher never ran; it is the opposite —
`rmSync` deleted the stamp and the grandchild rebuilt the tree around its own
write. An interim diagnosis of "not the refresher" was made on that misreading
and reversed on the residue.

**Root cause of why the leak looked already handled:** `stdin-resilience` and
`statusline-width` both seeded `pricing.json` with `data: {}` under a comment
claiming the render would then never consider a refresh. It does the reverse —
`loadPricingCacheEntry` discards an empty table (`pricing-cache.ts:66`), so the
render saw no cache and treated pricing as permanently stale. The seed was
inert; the comment was load-bearing and wrong.

| REL-004 | `ccfee31` | `readStdin`'s rejection now renders `formatStdinReadError` instead of falling through to `main().catch()`. Last input path that produced no output; deferred by #87, never filed. 2 sabotages |
| OPS-006 | `6f10d19` | `GCCUSAGE_DEBUG=1` surfaces whatever `main().catch()` swallowed, on stderr. Off by default, pinned by a test. README Troubleshooting entry for the disappeared bar + the two undocumented env vars. 3 sabotages. `index.ts` 100% statements |

**All findings from the 2026-08-05 grade are now closed.** 911 → 922 tests. CI
green on every commit since the repo went public.

**The one cap still notionally live is `enforce_admins: false`** — CI is
required for PRs and contributors but the sole maintainer can push directly to
`main` (and does; `remote: Bypassed rule violations` means the push
*succeeded*). Flipping it to `true` is a one-line change and the only remaining
argument against "mandatory CI".

**A test that passes against the pre-fix code is not automatically vacuous.**
REL-004's middle assertion does exactly that — the old code also skipped
`parseStatusJson`, by escaping the function entirely — but it catches
catch-then-continue, which would render the degraded bar *and* cache it under
the empty payload's key. Verified by that specific sabotage, and the
distinction is now written at the assertion rather than left for a later reader
to rediscover. Same lesson as the `data: {}` comment above: the trap is a
comment that describes a guard the code does not provide.

## Audit — 2026-08-05 @ 7e6becd
Mode: grade
Score: 86/100 (B)  Caps applied: **no-mandatory-CI (max B, binding)**; no-observability-story (max B+, non-binding at this score)

Baseline: branch `main`, worktree clean (only this untracked `AUDIT.md`), Node v26.5.0,
npm 11.17.0. Scope: the 33 commits `d341c27..7e6becd` — the full remediation of the
20 findings filed by the previous audit.

Executed (all rc captured, not eyeballed): `npm run typecheck` (0), `npm run
typecheck:scripts` (0), `npm run lint` (0, clean), `npm run build` (0, `dist/index.js`
byte-identical to the committed artefact — md5 `5998570410fa27dfdcfd566439a5bd54`),
`npm test` (911 tests / 56 files; 5/5 green on repeat runs, **1 flake reproduced under
parallel load**), `npm run coverage` (97.77% statements, per-file 70% gate green),
`npm audit` (9: 2 low / 7 high, all build-chain) and `npm audit --omit=dev`
(1 moderate, valibot `flatten()` — verified unreachable by grep, not by version),
`npm pack --dry-run` (4 files, 68 kB), and the real production path against the
shipped bundle: render (both lines, exit 0, **40 ms to process exit** with a
blackholed pricing endpoint), `setup` (writes the Homebrew symlink not the Cellar
path; idempotent), `today`, unknown-subcommand (rc=1, stderr), and all four
degraded-input paths (invalid JSON / empty / array root / null root).

Three reliability failure timelines constructed and executed:
1. **8 concurrent sessions recording spend** → 8 shards, exactly $8.00. REL-001 holds.
2. **40 SIGKILLs mid-persistence** → 0 corrupt files, 0 orphaned temp files, renders
   correctly afterwards. Atomic-write posture holds.
3. **Pricing endpoint blackholed** → 40 ms to exit. REL-002 holds (was ~10.5 s).

Fixed since previous review: all 20 findings (#81–#100) verified closed against their
original evidence, plus the 18 spun-off issues (#103–#137). Root causes, not symptoms:
`writeFileSync` appears exactly once in non-test `src/`; the non-finite class was closed
at the schema boundary rather than per-formatter; `metrics.today` was deleted rather
than left reading zero.

Deferred: none by the user — all seven findings below are new and unremediated.

Accepted risks: unchanged from the ledger above (#91's >10× price move; #125's latent
tier-stripping; the exact-key pricing poisoning residual).

### New findings

| ID | Pri | Title |
|---|---|---|
| TEST-002 | P1 | Detached pricing refresher unsuppressed in 5 spawn-test files — flake, 1,520 leaked dirs, live network from the suite |
| OPS-005 | P1 | No mandatory CI: the workflow has never executed once (12 merges) and branch protection is unavailable on this plan |
| OPS-006 | P2 | Render path has zero observability — silent bar erasure is undiagnosable |
| REL-004 | P2 | stdin stream `error` path erases the bar with no diagnostic (deferred by #87, never filed) |
| OPS-007 | P3 | No git tags, no releases, not on npm; version 0.2.0 unchanged across 33 commits |
| DOC-001 | P3 | README build badge stale at `0802.8`, 15 commits behind (downstream of the billing block) |
| CLI-001 | P3 | `--help` / `-h` / `--version` / `-v` unrecognized → exit 1; only bare `help` works |

```json
{
  "audit_date": "2026-08-05", "commit": "7e6becd", "overall_score": 86, "overall_grade": "B",
  "production_ready": false, "senior_quality": true,
  "applied_grade_caps": ["no_mandatory_ci"],
  "category_scores": { "product_coherence": 86, "architecture": 89, "correctness": 90,
    "security_privacy": 90, "reliability_concurrency": 84, "testing_ai_quality": 86,
    "maintainability": 92, "operations_observability": 62, "documentation_governance": 88 },
  "release_blockers": [],
  "p0_count": 0, "p1_count": 2, "p2_count": 2, "p3_count": 3,
  "fixed_since_previous_review": ["REL-001","COR-001","COR-002","REL-002","OPS-001",
    "COR-003","REL-003","OPS-002","OPS-003","OPS-004","SEC-001","SEC-002","COR-004",
    "PERF-001","TEST-001","CACHE-001","CLEAN-001","MAINT-001","CLEAN-002","MAINT-002"],
  "remaining_existing_issues": [],
  "new_findings": ["TEST-002","OPS-005","OPS-006","REL-004","OPS-007","DOC-001","CLI-001"],
  "top_three_next_actions": [
    "Restore GitHub Actions billing so the authored 5-job workflow actually runs, then require it on main",
    "Sweep the #122 refresher suppression across all five spawn-test files; assert zero leaked temp dirs",
    "Add a GCCUSAGE_DEBUG escape hatch so a silently-erased bar can be diagnosed"
  ]
}
```

## Audit — 2026-08-01 @ d341c27
Mode: grade
Score: 73/100 (C)  Caps applied: none binding (no-mandatory-CI and race-prone-processing both cap at B, above the raw score)

Baseline: branch `main`, worktree clean, Node v26.5.0, npm 11.17.0.
Executed: `npm run typecheck` (pass), `npm run typecheck:scripts` (pass), `npm test`
(440 tests / 30 files, pass), `npm run build` (bundle byte-identical to the committed
`dist/index.js`), `npm audit`, coverage via a temporary `@vitest/coverage-v8` install
(87.78% statements, removed afterwards), and the real production path — the shipped
bundle driven by stdin payloads.

Fixed: none (first audit — read-only grade mode).

Deferred: all findings below; no remediation requested.

Accepted risks: none recorded yet.

### Findings summary

All findings filed as GitHub issues #81–#100 on 2026-08-01, labelled `audit-P1`/`audit-P2`/`audit-P3`.

| ID | Pri | Issue | Title |
|---|---|---|---|
| REL-001 | P1 | #81 | Concurrent sessions permanently lose today's spend (no atomic write / locking) |
| COR-001 | P1 | #82 | Network-unavailable → $0.00 cost for every current model (stale fallback table) |
| COR-002 | P1 | #83 | One stdin field type change collapses the whole bar to $0.00 |
| REL-002 | P1 | #84 | 10.6s render stall when the pricing endpoint blackholes |
| OPS-001 | P1 | #85 | No CI runs tests, typecheck, or build |
| COR-003 | P2 | #86 | Wide characters undercounted → bar overflows the terminal |
| REL-003 | P2 | #87 | 1s stdin timeout silently renders an empty bar |
| OPS-002 | P2 | #88 | `setup` exits 0 having done nothing on a null/scalar settings.json |
| OPS-003 | P2 | #89 | `setup` non-atomically overwrites `~/.claude/settings.json` with no backup |
| OPS-004 | P2 | #90 | `process.execPath` pins a versioned Node path that Homebrew upgrades delete |
| SEC-001 | P2 | #91 | Unpinned third-party pricing feed drives cost math with no integrity check |
| SEC-002 | P2 | #92 | Cache files parsed with `as` casts, unvalidated (config, by contrast, is validated) |
| COR-004 | P2 | #93 | Pricing cache persists the un-merged table; fallback models vanish after run 1 |
| PERF-001 | P2 | #94 | 35 MB of transcript re-read and re-parsed on every cache miss |
| TEST-001 | P2 | #95 | Coverage collapses exactly at the I/O boundaries where every real defect was found |
| CACHE-001 | P2 | #96 | Statusline cache key omits inputs that change the bar (vim mode, branch, context %) |
| ~~CLEAN-001~~ | P3 | #97 | ~~`maxWidth` overloaded as a cache TTL; `config-schema.json` contradicts the README~~ — CLOSED, PR #134; renamed to `cacheTtlMs` and removed, plus a guard tying the schema's option list to valibot by name |
| MAINT-001 | P3 | #98 | No linter or formatter in the toolchain |
| ~~CLEAN-002~~ | P3 | #99 | ~~`trackTurn` writes a file on every render for a widget no default layout renders~~ — CLOSED, PR #128 |
| ~~MAINT-002~~ | P3 | #100 | ~~Five near-identical hand-rolled cache-persistence blocks~~ — CLOSED 2026-08-04, verification only; spun off #133 |

```json
{
  "audit_date": "2026-08-01", "commit": "d341c27", "overall_score": 73, "overall_grade": "C",
  "production_ready": false, "senior_quality": false, "applied_grade_caps": [],
  "category_scores": { "product_coherence": 78, "architecture": 82, "correctness": 70,
    "security_privacy": 78, "reliability_concurrency": 55, "testing_ai_quality": 74,
    "maintainability": 82, "operations_observability": 58, "documentation_governance": 84 },
  "release_blockers": ["REL-001", "COR-001", "COR-002", "REL-002", "OPS-001"],
  "p0_count": 0, "p1_count": 5, "p2_count": 11, "p3_count": 4,
  "fixed_since_previous_review": [], "remaining_existing_issues": [],
  "new_findings": ["REL-001","COR-001","COR-002","REL-002","OPS-001","COR-003","REL-003",
    "OPS-002","OPS-003","OPS-004","SEC-001","SEC-002","COR-004","PERF-001","TEST-001",
    "CACHE-001","CLEAN-001","MAINT-001","CLEAN-002","MAINT-002"],
  "top_three_next_actions": [
    "Add a CI workflow running typecheck + test + build + bundle-drift check on PR and main",
    "Make every cache write atomic (temp file + rename) and validate on read",
    "Refresh the offline pricing fallback and surface degradation instead of rendering $0.00"
  ]
}
```
